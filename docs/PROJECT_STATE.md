# دفتر حالة المشروع ووضع الاستئناف (PROJECT_STATE)

> **هذه الوثيقة هي ذاكرة الاستئناف الرسمية.** إذا انتهت جلسة التطوير أو حُذف سياق المحادثة، كل ما يلزم لاستئناف العمل عليها موجود هنا + في [`docs/DEVELOPMENT_LOG.md`](./DEVELOPMENT_LOG.md) (سجل المهام الكامل بالتفصيل).
>
> آخر تحديث: **2026-09-25** — الإصدار الحالي: **v2.4** (الويب) / **APK v4.0** — GitHub main: `a51dd3a`

---

## 1) ما هو المشروع

تطبيق **دردشة فوري** (Instant Chat) — Next.js 16 (App Router) + TypeScript + Socket.IO مدمج في خادم Next مخصص (`server.ts`) + Prisma/SQLite + Tailwind/shadcn/ui.

- **تسجيل الدخول برمز OTP ثنائي القناة**: تيليغرام (البوت `@instant_chat_otp_bot`) أو رسالة SMS حقيقية عبر بوابة relay على هاتف أندرويد للمالك (أو Twilio/Vonage/HTTP إن أُضيفت لاحقاً).
- **واجهات متعددة**: تطبيق ويب كامل (PWA قابل للتثبيت) + نسخة خفيفة `public/legacy.html` لأقدم المتصفحات (REST فقط بلا WebSocket) + تطبيق أندرويد `android/InstantChat.apk` (Cordova بواجهة إطلاق ES5 تفتح النسخة الخفيفة).

## 2) الروابط المهمة

| العنصر | الرابط |
|---|---|
| الإنتاج (Render) | https://instant-chat-f2ac.onrender.com |
| GitHub (الفرع main) | https://github.com/ehessan1974-maker/instant-chat |
| تحميل ZIP كامل | https://github.com/ehessan1974-maker/instant-chat/archive/refs/heads/main.zip |
| النسخة الخفيفة | https://instant-chat-f2ac.onrender.com/legacy.html |
| صفحة التحميل (APK + ويب + QR) | https://instant-chat-f2ac.onrender.com/download.html |
| الريبو الخاص (مفاتيح + أدوات + ذاكرة) | https://github.com/ehessan1974-maker/instant-chat-private (private) |
| بوت تيليغرام | @instant_chat_otp_bot |
| ملف APK | `android/InstantChat.apk` داخل الريبو |
| شرح بوابة SMS relay | [`docs/sms-relay-setup.md`](./sms-relay-setup.md) |

## 3) آخر ما أُنجز (صفحة تحميل ويب + APK v4.0 «واجهة إطلاق ذكية»)

**الأحدث — سكربت التحديث الموحد للمستودعين (جلسة `update-all-1`):**

1. **`scripts/update-all.sh` في بيئة التطوير** (`/home/z/my-project/scripts/`، يُزامَن تلقائياً إلى الريبو الخاص): أمر واحد ينفّذ 5 خطوات — تأكيد التوكين وأن الخاص `private:true` عبر API → كوميت ودفع العام `instant-chat` → مزامنة الورشة (`.apk-signing` + scripts + download + worklog + لقطات) → فحص تسرب توكينات → كوميت ودفع الخاص `instant-chat-private`. يعيد في النهاية معرّفي الكوميت النهائيين.
2. **التوكين خارجي دائماً**: `-t ghp_XXX` أو متغير البيئة `GH_TOKEN` أو ملف `.gh-token` المحلي (خارج نطاق المزامنة) — لا يُكتب أبداً في `.git/config` ولا في أي ملف مرفوع، ورسائل الخطأ تُنقَّح منه قبل العرض. المالك يسحب التوكين بعد كل استخدام — لذلك كل رفع يحتاج توكيناً جديداً.
3. **أوضاع مساعدة**: `--dry-run` (معاينة كاملة بلا دفع ولا توكين) + `-m "نص"` (رسالة موحدة للكوميتين) + `-h`. السكربت القديم `sync-github.sh` صار غلافًا يوجّه إليه تلقائياً.

**قبلها — صفحة تحميل ويب للبرنامج (جلسة `download-page-1`):**

1. **صفحة ثابتة كلاسيكية** على `/download.html` (بالمصدر وفي `prebuilt/next/public/`): ترويسة خضراء بشعار التطبيق + زرّان (تحميل APK / فتح النسخة ويب) + رمز QR يشير للـAPK مباشرة + خطوات التثبيت الثلاث + تنبيه حذف الإصدار القديم. CSS كلاسيكي 100% (بلا @layer/oklch/flex-gap) — تعمل على أقدم المتصفحات وWebView المجمد مثل legacy.html تماماً. بلا JavaScript إطلاقاً.
2. **استثناءات proxy جديدة**: `download.html` + `InstantChat.apk` + `qr-download.png` أُضيفت لمطابقة الـproxy في المصدر (`src/proxy.ts`) وفي 3 ملفات مُجمّعة داخل prebuilt (ترقيع جراحي عبر `scripts/patch_matcher.py` — انظر القسم 5). السبب: بدونها يُحوَّل طلب التحميل من المتصفحات القديمة إلى legacy.html فيعذَر تحميل الـAPK من المتصفح — والقديمون أشد المحتاجين.
3. **الملفات تُخدم من الحزمتين معاً**: `public/` (مصدر) و`prebuilt/next/public/` (ما يعمل فعلياً على Render) — APK (3.47MB بنوع MIME الصحيح `application/vnd.android.package-archive`) + download.html + qr-download.png.
4. **تحقق تشغيلي فعلي**: خادم prebuilt المُرقّع شُغّل محلياً واختبر بـ7 حالات (قديم Chrome43 × حديث Chrome120): الصفحة والـAPK والـQR تصل للقديم بلا تحويل، وتحويل «/» إلى legacy.html للقديم سليم كما هو، والحديث يفتح التطبيق الكامل — كلها ✓ + لقطة شاشة للصفحة على 390x844.

**قبلها (APK v4.0 — «واجهة إطلاق ذكية تفتح التطبيق الكامل»):**

**ترقية تطبيق الأندرويد ليفتح التطبيق الكامل على الأجهزة القادرة مع بقاء التوافق مع القديمة:**

1. **فحص قدرات ذكي (ES5)**: الواجهة تفحص WebView عند الإقلاع (async + optional chaining + Promise.allSettled) — الجهاز الحديث (Chromium 80+) يفتح «/» التطبيق الكامل، والقديم يفتح `/legacy.html`. النتيجة محفوظة فلا يُعاد الفحص.
2. **خادم مضمّن تلقائياً** (Render) مع افتتاح ذاتي بعد 1.4 ثانية وزر «إلغاء» + ضغطة طويلة على الشعار تفتح إعدادات كاملة (وضع الفتح: تلقائي/كامل دائماً/خفيفة دائماً + تغيير الخادم).
3. **مفتاح توقيع جديد** (القديم فُقد مع بيئة سابقة): RSA-2048 صالح حتى 2076 — ⚠️ أول تثبيت يتطلب إلغاء تثبيت النسخة القديمة مرة واحدة. **نسخة احتياطية للمفتاح إلزامية** (`.apk-signing/README.md`).
4. **assetlinks.json** محدّث بقيد `com.chatapp.instant` بالبصمة الجديدة (إلى جانب قيد TWA القديم).
5. **دخول محفوظ**: موجود أصلاً في النسختين (ic_token/ic_me) — يعمل تلقائياً بعد أول تسجيل دخول.
6. تحقق شامل: 12 فحصاً مستقلاً (build+verify.py) + keytool VALID + اختبار متصفح فعلي للمسارين (حديث → التطبيق الكامل، محاكاة قديم → legacy).

قبلها (v2.4): تفعيل الدخول الحقيقي من داخل التطبيق (بطاقة المالك) + بوابة SMS relay + أمر Termux.

## 4) ما تبقّى بيد المالك (خطوات عند العودة — دقائق)

1. **مفتاح التوقيع**: ✅ منسوخ في الريبو الخاص `instant-chat-private` (commit 04e5773) — ويبقى النسخ **خارج الجهاز** (USB/وسيط آخر) مستحسناً. الملفات: `.apk-signing/apk-key.pem` و`apk-cert.pem` (التفاصيل: `.apk-signing/README.md`).
2. **ارفع آخر التغييرات بأمر واحد**: `bash /home/z/my-project/scripts/update-all.sh -t <توكن_جديد>` — يرفع العام والخاص معاً ويعرض معرّفي الكوميت (توكن كلاسيكي بصلاحية `repo`، واسحبه بعد الاستخدام). للمعاينة أولاً: أضف `--dry-run`.
3. **تأكد أن Render نشر آخر commit** (assetlinks الجديد يُخدم من `/.well-known/assetlinks.json`، وصفحة التحميل من `/download.html`).
4. **شارك رابط التحميل مع المستخدمين**: `https://instant-chat-f2ac.onrender.com/download.html` — يعمل على أي هاتف/متصفح حتى القديم.
5. **على الهواتف**: احذف APK القديم ثم ثبّت الجديد (توقيع جديد = إلغاء تثبيت إجباري مرة واحدة). بعد أول دخول الجلسة محفوظة.
6. **تفعيل قنوات OTP** (إن لم تكن مفعّلة): بطاقة «تفعيل الدخول الحقيقي» → توكن البوت → أمر Termux لبوابة SMS.
7. ملاحظة: قرص Render المؤقت (free tier) يمحو التهيئة عند إعادة النشر — أعد التفعيل من البطاقة (30 ثانية).

## 5) نقاط معمارية يجب ألا تُنسى

- **Socket.IO مدمج داخل خادم Next** عبر `server.ts` مخصص — منفذ واحد للجميع (محلي/Render/APK القديم على `/socket.io`). لا تعد لفصل الخدمات.
- **`src/lib/settings.ts`**: كل الإعدادات (توكن البوت، رمز بوابة SMS، اسم المستخدم للبوت) تُقرأ: بيئة أولاً ثم قاعدة البيانات `Setting`. أي refactor جديد يتبع النمط نفسه.
- **`POST /api/setup`**: تحقق `getMe` + قائمة بوتات مسموحة (`SETUP_EXTRA_BOTS` للتوسيع) + حد محاولات + حفظ + بدء بولينج فوري. `GET /api/setup` حالة التهيئة بلا أسرار.
- **`/api/auth/channels`** يعيد `{telegram: boolean, sms: 'relay'|'twilio'|'vonage'|'http'|'demo'|null}` — الواجهة تعتمد عليه لعرض الأزرار. `'demo'` فقط خارج الإنتاج أو مع `OTP_DEMO_MODE=1`.
- **الكولداون لكل قناة على حدة** في `request-otp` (حقل `channel` ضمن شرط findFirst).
- **ترويسات no-store** على الصفحة والmanifest وواجهات API — لمنع مشكلة «النسخة القديمة في المعاينة» (كانت كارثية سابقاً).
- **شارة الإصدار** في `src/lib/version.ts` (`APP_VERSION`) تظهر بشاشة الدخول والشريط العلوي — **أي تعديل ظاهر = رفع الإصدار** (التالية v2.5).
- **بناء الـ APK (v4.0)**: المصدر `.apk-signing/index.html` (واجهة الإطلاق الذكية) + `build.py` + `verify.py` + المفتاح — كلها **خارج الريبو** في بيئة التطوير (`.apk-signing/`). سلسلة العمل: عدّل index.html → `python3 build.py` → `python3 verify.py` (12 فحصاً) → انسخ `InstantChat.apk` إلى `android/`. نفس المفتاح = تحديث بلا إلغاء تثبيت.
- **الواجهة الذكية**: `ic_apk_cap` (نتيجة فحص القدرات) و`ic_apk_mode` (auto/full/legacy) و`ic_apk_server` في localStorage — مسح بيانات التطبيق يعيد الفحص من الصفر (غير ضار).
- **legacy.html** تحتاج خادماً (مسارات API نسبية) — لا تفتحها كملف مباشر.
- **⚠️ Render يشغّل `prebuilt/next` لا المصدر!** الـDockerfile ينسخ الحزمة المبنية مسبقاً (الخطة المجانية لا تتحمل بناء Next.js). أي تعديل يظهر للمستخدم يجب أن يصل إلى `prebuilt/next/` (ملفات public تُنسخ مباشرة؛ تعديلات الكود تحتاج إعادة بناء الحزمة محلياً). ملفات المصدر تُحدَّث أيضاً للاتساق.
- **استثناءات proxy (المطابقة)** موجودة في 4 مواضع يجب أن تبقى متطابقة: `src/proxy.ts` (المصدر) + 3 ملفات مُجمّعة داخل `prebuilt/next/.next/` (chunk `[root-of-the-server]__99d037c4._.js` + `_clientMiddlewareManifest.json` + `functions-config-manifest.json`). الترقيع الجراحي للملفات المُجمّعة: `scripts/patch_matcher.py` (في بيئة التطوير). إعادة بناء كاملة مستقبلاً تُنتجها من المصدر تلقائياً فلا ضرر من بقاء الترقيع.

## 6) قواعد العمل الدائمة (اتفقتُ عليها مع المالك)

1. **الرفع إلى GitHub فور كل إنجاز** («أريد الرفع دائماً كل لحظة بلحظتها») — لا تجميع مؤجل.
2. **«شوف شو الأنسب وعملو»** — اختيار الحل الأنسب وتنفيذه مباشرة دون سؤال.
3. كل مهمة تُوثّق في سجل التطوير (نسخته هنا `docs/DEVELOPMENT_LOG.md`) بصيغة: Task ID + ما تم + ملخص مرحلة.
4. لا توكنات ولا مفاتيح في الريبو إطلاقاً (تحقق سريع قبل كل رفع — يفرضه `update-all.sh` تلقائياً).
5. **الرفع للمستودعين بأمر واحد** عبر `update-all.sh` من بيئة التطوير — التوكين يُمرَّر خارجياً كل مرة ولا يُخزَّن في أي ملف مرفوع.

## 5+1) مشاكل معلقة خفيفة (غير عاجلة)

- مؤشر «غير متصل… جاري إعادة الاتصال» ظهر قديماً في المعاينة — لم يُشخّص جذرياً؛ إذا تكرر: افحص اتصال Socket.IO بعد دمج `server.ts`.
- إن ظهرت النسخة قديمة في المعاينة المحلية رغم الرفع: امسح كاش المتصفح (no-store مفعّلة لكن التطبيق PWA مثبّت قد يحتفظ بالنسخة حتى تحديث Service Worker).

## 7) كيف تستأنف التطوير (لأي جلسة جديدة)

1. اقرأ هذه الوثيقة ثم [`docs/DEVELOPMENT_LOG.md`](./DEVELOPMENT_LOG.md) (آخر 2–3 مهام تكفي عادة).
2. بيئة التطوير: مشروع Next.js 16 + Bun — شغّل خادم التطوير فقط عبر سكربت التهيئة المعتاد (لا `bun run build`). اقرأ `dev.log` عند الشك.
3. عدّل الكود → **ارفع الإصدار في `src/lib/version.ts`** → تحقق متصفحاً → commit بنسخة المزامنة → push إلى main.
4. وثّق المهمة في سجل التطوير وأعد نسخه إلى هذا الريبو (كما فعلت هذه الجلسة) ثم ارفع مرة أخيرة.

> **ملاحظة أمنية:** توكن بوت تيليغرام يُفعّل من داخل التطبيق (البطاقة) أو من متغيرات بيئة Render (`TELEGRAM_BOT_TOKEN` + `TELEGRAM_BOT_USERNAME=instant_chat_otp_bot` + اختيارياً `SMS_PROVIDER=relay`) — ولا يُكتب في أي ملف داخل الريبو.
